<Window x:Class="Phexor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:rules="clr-namespace:Phexor.Rules"
        Title="Phexor" MinWidth="900" MinHeight="550" Width="900" Height="550"
        Icon="..\Ressources\Icon.ico" x:Name="Window">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0"
                                          x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </Window.Resources>

    <Grid Background="{Binding BackgroundColor}">
        <!-- Haupt-Grid-Layout -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header (Textbox & Buttons) -->
            <RowDefinition Height="5" />   <!-- Separator -->
            <RowDefinition Height="*" />   <!-- Hauptinhalt -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" /> <!-- Verzeichnisse -->
            <ColumnDefinition Width="5" />  <!-- Vertikale Trennlinie -->
            <ColumnDefinition Width="5*" /> <!-- Dateien -->
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Grid.ColumnSpan="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />   <!-- TextBox -->
                <ColumnDefinition Width="Auto" /> <!-- Buttons -->
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                    BorderBrush="{Binding BorderColor, Source={StaticResource ApplicationSettings}}"
                    BorderThickness="1"
                    Background="{Binding BackgroundColor, Source={StaticResource ApplicationSettings}}" />
            <!-- TextBox -->
            <Grid Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <TextBox VerticalAlignment="Stretch"
                         HorizontalAlignment="Stretch"
                         Background="Transparent"
                         BorderBrush="Transparent"
                         Margin="5"
                         Padding="5"
                         FontSize="15"
                         Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}"
                         FocusVisualStyle="{x:Null}"
                         KeyDown="TextBox_KeyDown">
                    <TextBox.Text>
                        <Binding Path="CurrentPath" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <rules:PathValidationRule />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" /> <!-- Settings Button -->
                    <ColumnDefinition Width="Auto" /> <!-- Undo Button -->
                    <ColumnDefinition Width="Auto" /> <!-- Redo Button -->
                </Grid.ColumnDefinitions>

                <!-- Settings Button -->
                <Button Grid.Column="0" Margin="5"
                        Background="{Binding SpecialColor, Source={StaticResource ApplicationSettings}}" Width="30"
                        Height="30"
                        Command="{Binding OpenSettingsCommand}" FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Content="&#xE713;">
                </Button>

                <!-- Undo Button -->
                <Button Grid.Column="1" Margin="5"
                        Background="{Binding SpecialColor, Source={StaticResource ApplicationSettings}}" Width="30"
                        Height="30"
                        Command="{Binding UndoCommand}"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Content="&#xE7A7;">
                </Button>

                <!-- Redo Button -->
                <Button Grid.Column="2" Margin="5"
                        Background="{Binding SpecialColor, Source={StaticResource ApplicationSettings}}" Width="30"
                        Height="30"
                        Command="{Binding RedoCommand}"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Content="&#xE7A6;">
                </Button>
            </Grid>

        </Grid>

        <!-- Verzeichnisse (Links) -->
        <Border Grid.Row="2" Grid.Column="0"
                BorderBrush="{Binding BorderColor, Source={StaticResource ApplicationSettings}}" BorderThickness="1"
                Background="{Binding BackgroundColor, Source={StaticResource ApplicationSettings}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /> <!-- Header -->
                    <RowDefinition Height="*" />    <!-- Inhalt -->
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Verzeichnisse" FontSize="20" Margin="10"
                           Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}" />
                <ListBox Grid.Row="1" Margin="10"
                         Background="{Binding ControlBackgroundColor, Source={StaticResource ApplicationSettings}}"
                         Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}"
                         ItemsSource="{Binding Directories}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}"
                                       FontSize="14"
                                       Margin="5"
                                       Cursor="Hand">
                                <TextBlock.InputBindings>
                                    <MouseBinding
                                        Command="{Binding DataContext.OpenDirectoryCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"
                                        MouseAction="LeftClick" />
                                </TextBlock.InputBindings>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Separator (Vertikal) -->
        <Border Grid.Row="2" Grid.Column="1"
                BorderBrush="{Binding BackgroundColor, Source={StaticResource ApplicationSettings}}"
                BorderThickness="1,0,0,0" />

        <!-- Dateien (Rechts) -->
        <Border Grid.Row="2" Grid.Column="2"
                BorderBrush="{Binding BorderColor, Source={StaticResource ApplicationSettings}}" BorderThickness="1"
                Background="{Binding BackgroundColor, Source={StaticResource ApplicationSettings}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /> <!-- Header -->
                    <RowDefinition Height="*" />    <!-- Inhalt -->
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Dateien" FontSize="20" Margin="10"
                           Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}" />

                <ListBox Grid.Row="1" Margin="10"
                         Background="{Binding ControlBackgroundColor, Source={StaticResource ApplicationSettings}}"
                         Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}"
                         ItemsSource="{Binding Files}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5">
                                <!-- Icon -->
                                <TextBlock FontFamily="Segoe MDL2 Assets"
                                           FontSize="16"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}"
                                           Margin="0,0,10,0"
                                           Text="{Binding ., Converter={StaticResource FileTypeToSymbolConverter}}" />
                                <!-- Text -->
                                <TextBlock Text="{Binding}"
                                           FontSize="14"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding ForegroundColor, Source={StaticResource ApplicationSettings}}"
                                           Cursor="Hand">
                                    <TextBlock.InputBindings>
                                        <MouseBinding
                                            Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            MouseAction="LeftDoubleClick" />
                                    </TextBlock.InputBindings>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>